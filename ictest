#!/bin/bash

usage() {
    echo "Usage: ictest [generate|run]" 1>&2
    exit 1
}

if [[ "$#" -ne 1 ]]; then
    usage
fi

case $1 in
"generate")
    ictest_generate
    ;;
"run")
    ictest_run
    ;;
*)
    echo "Unknown option '$1'" 1>&2
    usage
    ;;
esac
